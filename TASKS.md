# Pocket Forge 작업 현황

마지막 갱신: 2026-08-06

## 현재 작업

### Task 16 — 최적화와 QA

상태: 1차 완료 · 실기기 빌드 검증 대기

측정 먼저:

- HUD 전체 `Render()`는 0.099ms/호출로 측정돼 프레임 병목이 아니었다. 매 프레임 미션 보드를 재계산한다는 최초 가정은 측정으로 기각했다(2회 호출 0.007ms).
- 실제 비용은 `Resources` 폴더였다. 이 폴더는 참조 여부와 무관하게 전량 빌드에 포함되는데 UI 텍스처만 25.10MB였다.
- Task13 텍스처 75개 전부에 Android 임포트 오버라이드가 없어 압축 포맷이 지정되지 않은 상태였다(Final 52개·V5 38개는 이미 ASTC_6x6 지정).

완료 내용:

- 어떤 스크립트에서도 이름으로 참조하지 않는 Resources 텍스처 27개를 삭제했다(Final 6, V5 1, Task13 20 · 소스 3.88MB). 대부분 Task 13 정리에서 `...Clean` 자산으로 교체된 이전 원본과, 사용자가 제거한 선택 탭 자산이다. 원본은 `Art/`와 git에 남아 있다.
- 남은 Task13 텍스처 55개에 Android `ASTC_6x6`·CompressedHQ 오버라이드를 적용했다. 최대 크기는 원본 크기 이상의 2의 거듭제곱으로 잡아 화질 저하 없이 포맷만 바꿨다.
- 피드백 토스트에 남아 있던 하드코딩 영문 `CORE` 3곳을 `blueprint_core_short` 키로 현지화했다(Task 15 P2에서 상점·이벤트만 처리했던 누락분).

측정 결과:

- Resources 소스 25.10MB → 21.22MB
- Task13 텍스처 7.5MPixel 기준 압축 용량 추정 7.15MB(8bpp) → 3.18MB(ASTC 6x6), 약 3.97MB 절감
- 전체 Resources 텍스처의 ASTC 6x6 환산 용량은 약 7.02MB

검증:

- Unity 컴파일 오류 0건, 전체 EditMode 213/213 통과
- 신규 `ResourceBudgetTests` 3종: 코드가 이름으로 부르지 않는 Resources 텍스처 금지, 모든 Resources 텍스처의 명시적 ASTC_6x6 Android 포맷, 압축 환산 9MB 예산 상한
- 신규 `SaveCompatibilityTests` 14종: 저장 버전 1~12 각각이 `{"version":N}`만 남은 최악의 구버전 상태에서도 플레이 가능한 상태로 로드되는지, 음수·과대값 손상 저장의 정규화, `Normalize` 멱등성
- 자산 삭제 후 장비·박물관 1440×3088 캡처와 스프라이트 누락 0건을 확인
- **미검증**: 실제 AAB 용량 절감은 추정치다. 기본 검증 정책상 Android 빌드를 실행하지 않았으므로 서명 AAB 재빌드로 확인이 필요하다.

남은 항목:

- 서명 Release AAB 재빌드로 실제 용량(직전 57.82MB 대비) 확인
- 실기기 프레임·발열·메모리 측정은 실기기 검증 승인 후 진행

### Task 14-3 — 중장기 성장과 세션 밸런싱

상태: 완료

측정으로 확인한 문제:

- 광석 정의가 stage 10(crystal)까지만 있어 챕터 2~3이 crystal의 완만한 선형 곡선에 얹혀 있었다. stage 9는 내구도 160인데 stage 11은 67로, 챕터를 넘기면 일반 광석이 58% 약해지는 역전이 있었다.
- 보상은 stage에 선형인데 강화 비용은 1.55^레벨 기하급수라, 챕터가 진행될수록 경제가 비용 곡선을 따라가지 못했다.
- 그 결과 아이들 경로는 챕터 2에 52.3분이 걸리고 챕터 3은 2시간 안에 클리어하지 못했다. 방치형의 주 경로가 사실상 막혀 있었다.
- 보스 권장 채굴력이 4.0 → 15.17(3.8배) → 26.67(1.76배)로 챕터 1→2 구간에만 절벽이 있었다.

완료 내용:

- `ProgressionBalanceSimulator`를 다챕터로 확장(`SimulateChapters`)하고 챕터별 소요 시간·보스 실패·광석 수·획득 Credits·권장 채굴력을 각각 기록하는 `ChapterProgressResult` 추가
- `ChapterDefinition.rewardMultiplier`를 신설하고 `GetOreReward`에 반영해, 광석 정의를 늘리지 않고 챕터 단위로 보상 곡선을 세울 수 있게 함(신규 광석 추가는 박물관 2×2 그리드를 넘치게 하므로 배제)
- 챕터 2 보상 `×1.5`·보스 내구도 `×3.5 → ×1.65`, 챕터 3 보상 `×4`·보스 내구도 `×4.0 → ×1.85`, crystal 광석의 스테이지당 내구도 `7 → 14`로 조정
- 전면 광고 빈도를 카탈로그 데이터로 분리하고 신규 플레이어 보호 구간을 추가 — 간격 5광석 유지, 쿨다운 `180 → 240초`, 신규 유예 `600초`

측정 결과 (조정 전 → 후):

| 항목 | 이전 | 이후 |
|---|---:|---:|
| 아이들 챕터 2 | 52.3분 | 27.3분 |
| 아이들 챕터 3 | 2시간 내 미클리어 | 56.1분 |
| 능동 챕터 2 | 17.1분 | 10.4분 |
| 능동 챕터 3 | 38.8분 | 12.5분 |
| 보스 권장 채굴력 | 4.0 / 15.17 / 26.67 | 4.0 / 11.0 / 20.97 |

- 챕터 1은 능동 8.0분·아이들 16.4분으로 Task 14-1 기준값을 그대로 유지한다.
- 능동·아이들 모두 챕터 길이가 단조 증가하고 각 챕터에서 보스 실패 1회 관문이 유지된다.
- 4시간 오프라인 보상은 챕터 3 기준 633K Credits로, 같은 시점 드릴 강화 비용 48K 대비 복귀 세션의 의미를 유지한다.

검증:

- Unity 컴파일 오류 0건, 전체 EditMode 196/196 통과
- 신규 `ChapterProgressionBalanceTests` 7종: 능동·아이들 3챕터 완주와 길이 단조 증가, 챕터별 보스 관문 유지, 보스 권장치 증가폭 1.5~3.0배 제한, 스테이지 내구도 역전 방지, 챕터 보상 배율 증가, 광고 유예·쿨다운 하한
- 전면 광고 유예 회귀 1종 추가, 기존 보스 게이트 기대값 갱신
- 기본 검증 정책에 따라 Android APK/AAB·실기기 검증은 미실행

후속 후보:

- 챕터 2~3 전용 광석 아트와 정의를 추가할 경우 박물관 그리드를 3행 이상으로 확장해야 한다.
- 장비·연구·도감 기여도는 현재 곡선에서 목표 범위를 만족해 이번 조정에서 변경하지 않았다.

### Task 14-2 — 메타 경제와 선택형 가속 밸런싱

상태: 완료

완료 내용:

- `ProgressionBalanceSimulator`에 총생산·총소비 Credits 측정(`TotalCreditsEarned`/`TotalCreditsSpent`)을 추가 — 측정값은 능동 546 생산/214 소비, 자동 799 생산/437 소비로 2026-08-06 사전 감사와 일치
- 상점: 일일 무료 `125 Credits`(코어 제거), 광고 보급 `200 Credits × 일 3회`, `5 Gem → 600 Credits`, `10 Gem → 코어 3개`, 스타터 패키지 `2,000 Credits + 20 Gem + 코어 6개`
- 일일 미션 Credits `100 / 75 / 125`(코어 미션을 Credits 미션으로 전환, 보스 Gem 1 유지), 일일 완료 보상 코어 `2개 → 1개`
- 주간 미션 Credits `600 / 800`, 코어 `3개 → 2개`, Gem `5개 → 3개`
- 이벤트 누적 보상 `250 Credits / 코어 2개 / Gem 3개`, 교환 `20토큰 → 코어 1개`, 주 2회 유지
- 업적 Credits: 채굴 `75 / 300 / 1,500`, 시설 `100 / 500 / 2,000`, 장비 `150 / 600 / 2,500`
- 신규 `MetaEconomyBalanceTests` 5종: 총생산·소비 보존, 무료+광고 일일 Credits가 능동 챕터 생산의 0.5~1.5배, Gem 교환 1.25배·스타터 4배 상한, 보스 제외 주간 반복 코어 공급 상한 13개, 연구 총비용(59코어) 대비 연구 수명 4~5.5주 고정
- 모든 조정은 각 정의의 `CreateRuntimeDefaults()`와 `MissionService` 완료 보상 상수에만 적용 — 저장 스키마·UI 구조·광고·IAP 처리 로직은 변경하지 않음

검증:

- Unity 컴파일 오류 0건, Console 오류 0건(TestResults.xml 경로 안내 로그 제외)
- 구 수치를 하드코딩한 기존 테스트 7개 파일 갱신 후 전체 EditMode 184/184 통과
- 조정 후 주간 반복 코어 공급: 일일 완료 7 + 주간 미션 2 + 이벤트 누적 2 + 이벤트 교환 2 = 13개(목표 달성), 연구 수명 59/13 ≈ 4.5주
- 기본 검증 정책에 따라 Android APK/AAB·실기기 검증은 미실행

### Task 14-1 — 초반 성장 기준선·챕터 1 밸런싱

상태: 완료

완료 내용:

- 실제 `MiningGameService`의 채굴·강화·경험치·도감·보스 실패/재도전 규칙을 재생하는 결정론적 `ProgressionBalanceSimulator` 추가
- 희귀 광석 운을 제외한 보수적 기준에서 능동 플레이와 자동 진행의 첫 강화·첫 보스·완료 시간·구매 횟수·최종 시설 레벨을 산출
- 초기 자동 채굴력과 탭 피해를 조정해 신규 플레이어 탭 가속을 자동 생산 대비 40% 이내로 제한
- 챕터 1의 구리·철·금 광석 내구도, 크리스털 보스 내구도, 곡괭이·드릴·로봇 가격 곡선과 효과를 1차 조정
- 기본 상태로 첫 보스를 돌파할 수 없고, 첫 실패 뒤 일반 9스테이지 파밍과 시설 투자를 거쳐 재도전하도록 관문 구성

측정 결과:

- 능동 플레이: 첫 강화 32.6초, 첫 보스 261.0초, 챕터 완료 482.8초(8분 3초), 보스 실패 1회
- 자동 진행: 첫 강화 113.6초, 첫 보스 384.6초, 챕터 완료 983.6초(16분 24초), 보스 실패 1회
- 능동 최종 시설: 곡괭이 5 / 드릴 3 / 로봇 1, 채굴력 4.532 / 권장 4.000
- 자동 최종 시설: 곡괭이 0 / 드릴 5 / 로봇 3, 채굴력 4.275 / 권장 4.000

검증:

- 밸런스 대상 테스트 3/3, 채굴·보스 포함 영향 범위 36/36 통과
- 전체 EditMode 179/179 통과
- Unity 컴파일 오류 0건
- 사용자 요청에 따라 Android APK/AAB·실기기 빌드 검증은 실행하지 않음

다음 세부 작업:

- Task 14-2에서 상점·광고·Gem 교환·스타터 패키지·미션·이벤트 보상을 초반 생산량과 비교해 재화 공급·소비를 조정한다.
- Task 14-3에서 챕터 2~3 성장 곡선, 오프라인 생산, 장기 세션 길이와 광고 빈도를 함께 조정한다.

기본 검증 정책:

- 이후 작업은 Unity 컴파일과 관련 EditMode 회귀를 기본 검증으로 사용한다.
- Android APK/AAB와 실기기 검증은 사용자가 다시 명시적으로 요청할 때만 실행한다.

### Task 13 사용자 Play Mode HUD 배치 영구 반영

상태: 완료

완료 내용:

- 1440×3088 Play Mode 화면의 SafeArea 하위 RectTransform 125개를 전수 기록하고 코드 기본 생성값과 비교
- 구조상 변경된 66개 경로를 기준으로 상단 HUD, 액션, 업그레이드 카드, 하단 내비게이션 좌표·크기를 영구 반영
- 사용자가 삭제한 채굴 문구, 선택 탭, 내비게이션 문구 6개, 업그레이드 이름 3개를 생성 코드에서도 제거
- 미션 버튼을 전투력 패널 아래 위치 `455,1529`, 144×144로 유지하고 하단은 5개 슬롯 배치로 변경
- `ProgressFill`을 왼쪽 원점 유지 상태에서 488×53에서 468×53으로 축소
- 삭제된 내비게이션 텍스트 참조를 순회하던 코드를 제거해 반복 `MissingReferenceException` 해결

검증:

- Unity 컴파일 오류 0건, Console 오류 0건
- `MineHudResponsiveLayoutTests` EditMode 30/30 통과
- 전체 EditMode 154/154 통과
- 기준 스크린샷·전수 manifest·코드 기본값 diff를 `Review/CurrentPlayModeSnapshot_2026-08-02/`에 보관
- Play Mode 재실행, Android APK/AAB, 실기기 검증은 이번 반영 범위에서 미실행

### Task 13 UI 겹침·9-slice 후속 교정

상태: 완료

완료 내용:

- Task13 반복 표면의 알파 경계와 코너를 다시 측정하고 호출부 임의 숫자를 단일 Border 카탈로그로 교체
- 장비 인벤토리 베이스·희귀도·선택 테두리를 같은 RectTransform과 Sliced 규칙으로 통일
- 장비 카드 Divider, 박물관 카드의 구워진 하단 패널·게이지, 박물관 요약의 내용 없는 장식 슬롯을 제거한 클린 자산 추가
- 장비 비교·연구 행을 전용 가로 패널로 교체하고 버튼 5종을 실제 최소 Rect에 맞게 해상도 정규화
- 미발견 박물관 카드의 진행 게이지를 숨겨 런타임 게이지가 의미 있을 때만 한 번 표시
- Task13 전체 자산을 Sliced·Simple·Filled·대체 미사용으로 다시 분류하고 4개 최종 렌더와 표를 `Review/Task13OverlapFix/`에 기록

검증:

- Unity 컴파일 오류 0건
- UI·화면비·4개 언어·Slice 카탈로그 대상 EditMode 31/31 통과
- 전체 EditMode 155/155 통과
- 1080×1920 및 연결된 세로 기기 비율에서 장비·연구·박물관·업적 실제 렌더 육안 검수
- Android APK/AAB·실기기 검증은 이번 범위에서 미실행

### Task 13-5A — 광물 박물관·업적

상태: 완료

완료 내용:

- 광석 4종의 발견·누적 채굴 수와 단계 보너스를 저장하는 버전 10 마이그레이션
- 최초 발견 +1%, 누적 25·100·500개마다 +1%인 광석별 도감 배율
- 도감 배율을 자동·탭·오프라인·보스 비교가 공유하는 `CollectionMultiplier`에 연결
- 채굴·챕터·시설·광부·연구·장비를 재사용하는 6개 3단계 업적과 중복 수령 방어
- Credits·Gem·설계도 코어 업적 보상과 포화 연산
- 광부 Lv.3 박물관 해금, V5 스타일 박물관·업적 탭과 4개 언어
- 온라인·오프라인 채굴 집계와 오프라인 자기 증폭 방지

검증:

- Unity 컴파일 오류 0건
- 전체 EditMode 151/151 통과
- Device Simulator의 1440×3088 동일 비율 세로 화면에서 광석 4행·업적 6행·수령 버튼이 모달 안에 표시됨을 확인
- 최종 Console 오류 0건
- Android APK/AAB와 실기기 검증은 승인 범위에서 제외

디자인 참고 산출물:

- 장비·박물관·업적의 1440×3088 완성 가스샷 3종을 기존 캡처와 분리해 생성
- 메인 HUD 배치 교정용 좌표·Safe Area·비겹침 프롬프트 TXT 작성
- 개인 Codex 스킬 `pocket-forge-ui-layout-guard`로 이미지 생성과 Unity 적용 전후의 겹침·잘림 검수 절차 고정
- 장비·박물관·업적 가스샷을 원본 크롭 제한 편집하는 요소별 무문자 자산 프롬프트 TXT 작성
- 가스샷은 승인·분해 전 디자인 참고용이며 현재 런타임 UI와 Task 13-5B 상태는 변경하지 않음

### Task 13-4~13-5A 가스샷 UI 적용

상태: 완료

완료 내용:

- 승인된 장비·박물관·업적 가스샷을 기준으로 무문자 표면·아이콘·상태 오버레이를 분리해 67개 런타임 Sprite로 구성
- 장비 화면을 4슬롯, 2×3 인벤토리, 희귀도·선택·장착·합성 상태, 비교 트레이와 독립 행동 버튼으로 재구성
- 박물관 화면을 2×2 전시 카드, 발견·잠금, 개별 마일스톤 진행도와 다음 도감 보상으로 재구성
- 업적 화면을 아이콘·목표·진행·보상·상태의 고정 5열과 수령·진행 중·완료 상태로 재구성
- 메인 HUD를 1440×3088 기준 Safe Area와 수직 밴드에 맞춰 재배치하고 16:9·19.5:9·20:9에서 하단 HUD 순서를 유지
- 모든 표면은 `Image.Type.Simple`, 진행 채움만 `Image.Type.Filled`로 유지하고 동적 텍스트·수치·다국어는 이미지에 굽지 않음

검증:

- Unity 컴파일 및 최종 Console 오류 0건
- 전체 EditMode 152/152, UI·화면비 대상 28/28 통과
- 1440×3088 메인 HUD·장비·박물관·업적 런타임 캡처와 메인 가스샷 50% 오버레이 검수 완료
- Android APK/AAB와 실기기 검증은 이번 승인 범위에서 제외

후속 시각 검수:

- 2026-08-01 사용자 캡처에서 메인 하단 내비게이션, 모달 타이틀·닫기, 장비 비교 트레이, 업적 고정 열, 박물관 잠금 카드, 연구 행의 추가 겹침·압축을 확인
- Task13 Sprite가 Border 0의 Simple 경로로 강제 확대되는 현행 구조와 잘못된 Sliced 전환을 함께 교정하는 `POCKET_FORGE_UI_OVERLAP_AND_9SLICE_CORRECTION_PROMPT.txt` 작성
- 프롬프트만 작성했으며 실제 RectTransform·Sprite Border·렌더 타입과 위 검증 결과는 아직 변경하지 않음

## 완료된 현재 작업

### Task 13-5B — 일일·주간 미션

상태: 완료

목표:

- 이미 구현한 장기 목표인 박물관·업적과 겹치지 않는 짧은 접속 목표를 만든다.
- 일일·주간 미션을 Pocket Forge 규모에 맞게 제한한다.
- 신규 보상은 현재 소비처가 있는 Credits·Gem·설계도 코어·장비만 사용한다.

완료 내용:

- 광부 Lv.5에 일일 4개·주간 4개 미션과 각 기간 완료 보상을 해금
- 기존 누적 채굴·시설 레벨·연구 레벨·장비 획득 값의 기간 시작 스냅샷 차이로 진행도를 계산하고 반복 보스 처치만 누적 카운터로 추가
- UTC 00:00 일일 갱신, 월요일 UTC 00:00 주간 갱신, 마지막 관찰 UTC보다 과거인 기기 시각은 과거 기간을 다시 열지 않는 로컬 역행 방어 적용
- 개별·완료 보상 수령 상태와 기간 기준선을 저장 버전 11로 저장·복원하고 중복 지급을 차단
- Credits·Gem·설계도 코어·장비의 기존 지급 경로만 사용하고 한영일중 문구와 미션 알림 배지를 연결
- 기존 Task13 모달·행·버튼·재화 아이콘으로 미션 UI를 마감하고 전용 배너·완료 상자·미션 아이콘은 후속 교체 후보로 기록

검증:

- Unity 컴파일 및 최종 Console 오류 0건
- 미션 도메인·시간 정책·저장 마이그레이션·4개 언어·행 비겹침을 포함한 전체 EditMode 163/163 통과
- 1080×1920 Play Mode 런타임 캡처에서 일일 4행, 고정 보상·수령 열, 완료 보상·닫기 버튼 간격 확인
- Android APK/AAB와 실기기 검증은 이번 승인 범위에서 제외

후속 UI 교체 후보:

- 일일·주간 전용 헤더 또는 배너
- 미션 종류별 전용 아이콘과 설계도 코어 보상 아이콘
- 기간 전체 완료 보상 상자·수령 가능 연출

이번 단계 제외:

- 서버 기반 시간 검증과 라이브 운영 이벤트
- 소셜·랭킹·길드·친구 기능
- 상점·광고·IAP 상품 확장
- 대규모 도감·업적·미션 콘텐츠와 전용 신규 그래픽
- 전체 UI 재디자인
- Android APK/AAB와 실기기 검증

완료 조건 달성:

- 수집·업적·미션 진행과 보상 수령이 저장·복원되고 중복 지급되지 않는다.
- 기기 시간 역행·갱신 경계 정책이 문서와 테스트로 고정된다.
- Unity 컴파일과 전체 EditMode 회귀 테스트를 통과한다.

## 최근 완료

### Task 13-6 — 상점·광고·IAP 상품·이벤트 확장

상태: 완료

완료 내용:

- 광부 Lv.6 상점에 일일 무료 상품 1종, 일일 3회 광고 상품, Gem 소비 상품 2종, 기존 `remove_ads`, 신규 `starter_pack`을 데이터 정의로 구성
- 광고 완료 콜백에서만 보상을 지급하고 일일 잔여 횟수를 먼저 검사하도록 `MineAdCoordinator` 확장
- `remove_ads`와 `starter_pack`을 각각 비소모성 IAP로 조회하고 저장 성공 뒤에만 Pending 주문을 승인하는 상품별 트랜잭션 경계 구현
- 광부 Lv.7 주간 이벤트 `Crystal Rush`에 5광석당 1토큰, 누적 보상 3단계, 이벤트 토큰 교환 1종·주 2회 제한 구현
- 누적 이벤트 진척과 소비 가능한 토큰 잔액을 분리해 교환이 누적 보상 진척을 감소시키지 않도록 구성
- 저장 버전 12에 일일 상품 수령·광고 횟수, 스타터 패키지 권한, 주간 이벤트 기준선·토큰·수령·교환 상태와 기기 시간 역행 방어 추가
- 상점 6행·이벤트 4행을 아이콘/제목·설명/수치/고정 행동 열로 구성하고 한·영·일·중 문구 연결

임시 수치:

- 일일 무료: 500 Credits + 설계도 코어 1
- 광고 보급: 1,000 Credits, 일 3회
- Gem 상품: 5 Gem → 5,000 Credits, 10 Gem → 설계도 코어 5
- 스타터 패키지: 10,000 Credits + 50 Gem + 설계도 코어 10
- 이벤트: 10/30/60 토큰 누적 보상, 20 토큰 → 설계도 코어 4 교환
- 모든 수치는 Task 14 밸런싱에서 조정한다.

검증:

- Unity 컴파일 오류 0건
- 상점·이벤트·IAP·광고·저장 v12·4개 언어·세로 행 비겹침을 포함한 전체 EditMode 177/177 통과, 불필요한 Unity Localization 제거 후 최종 회귀 176/176 통과
- 1080×1920 세로 Game View에서 상점 6행 전체가 모달·Safe Area 안에 표시됨을 육안 검수
- 서명 Release AAB `PocketForge-0.1.0.aab` 빌드 성공: 57,822,260 bytes(57.82MB), `bundletool validate`·JAR 서명·기존 복구 지문 일치 확인
- 사용하지 않는 Engine Diagnostics와 Unity Localization을 제거해 관련 Android 빌드 오류 로그를 정리함
- Google Mobile Ads 패키지의 iOS `.xcframework` Android 비호환 로그 1건은 빌드 성공과 Android 광고 의존성 포함에 영향을 주지 않는 패키지 경고로 기록
- 실제 `starter_pack` 구매는 Play Console 상품 생성·활성화 뒤 내부 테스트 트랙에서 별도 검증 필요

한계와 후속:

- 서버가 없는 일일·주간 시간 검증은 로컬 역행 방어이며 계정 단위 보안이나 여러 기기 동기화를 제공하지 않는다.
- 운영 가격, 영수증·환불 검증, 이벤트 종료 미수령 보상과 라이브 운영은 출시 백엔드/Play Console 단계다.
- 전용 상점·이벤트 그래픽과 모션은 기존 Task13 자산으로 임시 마감했으며 후속 UI 폴리싱 후보로 남긴다.

## Task 13 잔여 범위

| 단계 | 내용 | 상태 |
|---|---|---|
| 13-1 | 챕터·스테이지·보스·최초 클리어 보상 | 핵심 기능 완료 |
| 13-2A | 방치형 핵심 공식, 총 채굴력, 보스 관문 | 핵심 기능 완료 |
| 13-2B | 전체 자동 채굴력 기반 오프라인 진행 | 핵심 기능 완료 |
| 13-3A | 광부 등급·경험치·기능 해금 | 핵심 기능 완료 |
| 13-3B | 설계도 코어·소규모 영구 연구 | 핵심 기능 완료 |
| 13-4 | 4슬롯 장비, 등급, 합성·교체 | 핵심 기능 완료 |
| 13-5A | 광물 박물관, 단계형 업적 | 핵심 기능 완료 |
| 13-5B | 일일·주간 미션 | 핵심 기능 완료 |
| 13-6 | 상점·광고·IAP 상품·이벤트 확장 | 핵심 기능·서명 AAB 검증 완료 |

Task 13의 방향은 클릭 횟수 중심 진행을 자동 생산·투자·보스 관문 중심으로 바꾸는 것이다. 다른 게임의 콘텐츠를 복제하지 않고 자동화, 복귀 보상, 여러 성장축의 합산, 장기 목표 계층만 Pocket Forge의 채굴 루프에 맞게 재설계한다.

### Task 15 — UI 정합성 패스

상태: 완료 (P1·P2·P3)

배경: 2026-08-06 전체 화면 시각 검수에서 겹침·이탈·비율 불일치를 우선순위별로 정리했다. 검수 근거 캡처는 `Review/UiPass_Before_Full.png`(수정 전)과 `Review/UiPass_Final_Full.png`(수정 후)다.

P1 완료 내용:

- 광석 내구도 바: 채움이 프레임 테두리를 침범하던 문제를 `12_OreHealthFrame` 아트의 안쪽 오목 영역(908×110 아트 px)을 실측해 트랙·채움을 449×54로 정합. 내구도 수치는 소수점 대신 올림 정수 표시
- XP 클러스터: 방패 아이콘이 수치 첫 글자를 덮던 겹침 해소, `XP` 접미사 제거, BestFit 적용, 진행 바를 패널 위에 뜨던 위치에서 알약 안 하단 언더라인(96×8)으로 이동
- `보스 도전` 버튼 문구를 2줄 줄바꿈에서 1줄로 변경하고 BestFit 적용
- 상단 빈 알약 슬롯: 광고 `+` 버튼을 알약 중앙(308,-190)으로 이동하고 터치 영역을 100×62로 확장
- 연구 완료 배지를 렉트 코너가 아니라 실제 그려지는 카드 코너(-76,-10)에 정렬
- 연구·미션 내비 알림 배지를 아이콘 코너에 밀착

P1 검증:

- Unity 컴파일 오류 0건, 전체 EditMode 184/184 통과(레이아웃 핀 테스트 갱신 포함)
- 1440×3088 Play Mode 캡처로 수정 전후 육안 비교 완료
- Android APK/AAB·실기기 검증은 기본 검증 정책에 따라 미실행

모달 전수 검수 (2026-08-06, 1440×3088 Play Mode 캡처 `Review/Modal_*.png`):

- 장비: 합성 가능 마름모 배지가 카드 테두리를 넘던 문제를 (-40,-76) 인셋으로 교정 — 완료
- 미션: 행 피치를 250→200으로 압축(그려지는 행 높이 ~174 기준 간격 26), 기간 완료 보상 행을 마지막 행 바로 아래로 이동, 완료 보상 텍스트의 어색한 중간 줄바꿈을 비분리 공백으로 교정 — 완료
- 상점·이벤트·업적·박물관·연구: 전수 캡처 검수 결과 구조 양호, Task 14-2 적용 수치가 UI에 정상 표시됨을 확인. 연구 모달 하단 여백 과다와 상점 재화 표기 혼용은 P2로 이월

P2 완료 (2026-08-06, 검수 캡처 `Review/P2_*.png`):

- 헤더 정렬: 전투력 패널을 306→265 폭·x 353으로 조정해 챕터 패널과 같은 145 높이로 그려지도록 정합. 내부 텍스트 오프셋·박스도 같은 비율로 축소
- 버튼 통일: 주 행동 표면을 금색 알약(`ButtonAchievementClaimRuntime`) 하나로 통일. 연구 구매와 장비 장착/해제에 쓰이던 주황 육각형(`ButtonEquipmentEquipRuntime`)을 UI에서 제거
- 업그레이드 카드: 아트에 구워진 빈 게이지를 삭제하는 대신 각 시설의 현재 기여도(`탭 6.33` / `자동 9.2` / `보상 ×2.04`)로 채움. 기존 `tap`·`auto`·`reward` 로컬라이징 키를 재사용해 신규 키 없음
- 연구 모달: 카드 높이 1200→1040으로 줄여 행과 닫기 버튼 사이 약 240의 빈 공간 제거
- 상점·이벤트 재화 표기: 하드코딩된 영문 `CORE`를 신규 `blueprint_core_short` 키(코어/Core/コア/核心)로 교체해 4개 언어 현지화
- 박물관 닫기 버튼 높이 72→88로 확대해 모바일 터치 영역 확보

P2 검증:

- Unity 컴파일 오류 0건, 전체 EditMode 186/186 통과
- 신규 회귀 2종: 챕터·전투력 패널의 그려지는 높이 일치, 업그레이드 카드 기여도 텍스트의 레벨·비용 행 비침범
- 메인·연구·장비·상점·이벤트·박물관 1440×3088 캡처로 육안 확인

P3 완료 (2026-08-06, 검수 캡처 `Review/P3_*.png`):

- 재화 아이콘 규격화: 아이콘 아트의 종횡비 편차(코인 0.98·젬 1.11·코어 1.77)로 같은 정사각 박스에서 코어가 코인의 약 57% 면적으로만 그려지던 문제를, 그려지는 면적을 같게 맞추는 `SizeCurrencyIcon` 규칙으로 교정. 미션 행·미션 완료·업적 행 보상 아이콘에 적용. 상단 바는 아트에 구워진 알약 폭이 이미 꽉 차 확대 여지가 없어 제외
- 좌우 카드 규격 통일: 연구 바로가기 아이콘을 방치 보상 카드와 같은 112×92·y 8로 맞추고, 하단에 연구 배율 값 텍스트를 추가해 두 카드를 제목·아이콘·값의 동일한 3단 구조로 정렬
- 최소 폰트 규칙: `MineHudView.MinimumReadableFontSize = 18`을 기준으로 정하고 미달 4종(전투력 권장치 16, 전투력 라벨·업그레이드 기여도·장비 수량 배지 17)을 18로 상향

P3 검증:

- Unity 컴파일 오류 0건, 전체 EditMode 188/188 통과
- 신규 회귀 2종: 모달 5종을 연 상태로 4개 언어의 모든 활성 라벨이 18 이상으로 해석되는지 검사, 주간 미션 보상 아이콘 4개가 서로 다른 종횡비에도 같은 그려지는 면적을 갖는지 검사
- 4개 언어 실측 결과 BestFit로 18 미만까지 축소되는 텍스트는 없음을 확인

## 다음 작업

1. Task 16 잔여: 서명 AAB 재빌드로 실제 용량 확인, 실기기 프레임·발열 측정
2. Jenkins Android AAB 빌드 자동화
3. Google Play 비공개 테스트와 업데이트 검증
4. 출시 자료와 포트폴리오 문서 정리

## 완료된 큰 단계

- Task 0~7: 개발 기반, 아키텍처 분리, 데이터 카탈로그, 저장·오프라인 보상
- Task 8~11: 모바일 HUD, 다국어, 피드백, 기존 콘텐츠 확장
- 광고: Google 공식 테스트 광고의 보상형·전면 광고 실기기 검증 완료
- IAP: `remove_ads` 비소모성 상품의 내부 테스트 구매·복원·재설치 복구 검증 완료
- Task 12: AI 생성 UI·Meshy 광석 적용, 화면비 대응, Android 용량 목표 검증, 사용자 승인 수준까지 폴리싱

세부 변경 이력과 검증 근거는 `WORKING_CONTEXT.md`, AI 산출물과 검증 구분은 `AI_USAGE.md`를 기준으로 한다.
