# RULES.md

이 문서는 Codex 또는 작업자가 프로젝트를 수정할 때 반드시 따라야 하는 사용 규칙이다.

목표는 무분별한 수정, 의도와 다른 대규모 변경, 작업 맥락 누락을 방지하는 것이다.

---

## 1. 기본 원칙

- 사용자의 명시적인 허락 없이 파일을 수정하지 않는다.
- 수정하기 전에는 반드시 수정 계획을 먼저 제시한다.
- 사용자가 확인하고 허락한 뒤에만 실제 작업을 시작한다.
- 이미 사용자가 답변한 내용을 다시 묻지 않는다.
- 모호한 부분이 있더라도 기존 기획과 문맥으로 판단 가능한 경우에는 합리적인 기본값을 사용한다.
- 추측으로 대규모 구조 변경을 하지 않는다.
- 작업 범위를 벗어난 파일은 건드리지 않는다.
- 요청받은 작업과 관계없는 리팩터링은 하지 않는다.
- 외부 패키지 추가가 필요하면 먼저 이유를 설명하고 허락을 받아야 한다.
- Placeholder 구현은 허용하지만, 반드시 Placeholder라고 명시한다.

---

## 2. 작업 시작 전 필수 출력 형식

파일을 수정하기 전에는 반드시 아래 내용을 먼저 출력한다.

```markdown
## 작업 전 확인

### 요청 요약

- 사용자가 요청한 작업을 짧게 정리한다.

### 수정 전 파일 / 시스템 요약

- 수정 대상 파일 또는 시스템의 현재 구조를 요약한다.
- 관련 파일이 아직 없다면 새로 생성할 파일을 명시한다.

### 수정 예정 내용

- 어떤 파일을 만들거나 수정할지 적는다.
- 각 파일에서 어떤 내용을 변경할지 적는다.
- 예상되는 동작 변화를 적는다.

### 작업 범위

- 이번 작업에 포함되는 항목을 적는다.
- 이번 작업에 포함하지 않는 항목을 적는다.

### 확인 요청

위 내용대로 수정해도 되는지 사용자에게 확인을 요청한다.
사용자가 명확히 허락하기 전까지 실제 파일 수정은 하지 않는다.
```

---

## 3. 사용자 허락 후 작업 규칙

- 수정 범위를 벗어난 파일은 변경하지 않는다.
- 기존 코드 스타일을 최대한 유지한다.
- 새 스크립트는 역할이 명확한 이름으로 만든다.
- 한 파일에 너무 많은 책임을 넣지 않는다.
- Inspector에서 조절해야 하는 값은 가능한 한 SerializeField 또는 ScriptableObject로 노출한다.
- 매직 넘버는 가능하면 설정값으로 분리한다.
- Unity 프로젝트에서는 씬 오브젝트 연결이 필요한 경우, 작업 후 연결 방법을 반드시 설명한다.
- 임시 구현이나 테스트용 코드는 명확히 표시한다.
- 오류 가능성이 있는 부분은 숨기지 말고 알려준다.

---

## 4. 작업 완료 후 필수 출력 형식

작업 완료 뒤에는 수정 전·후 요약, 추가·수정·삭제 파일, Unity Inspector 설정, 테스트 방법, 확인된 사항, 미확인/남은 작업을 포함한 `## 작업 완료 보고`를 작성한다.

---

## 5. WORKING_CONTEXT.md 갱신 규칙

- 기존 기록을 삭제하지 않는다.
- 가장 아래에 새로운 `수정사항 N` 항목을 추가한다.
- N은 이전 수정사항 번호보다 1 큰 숫자를 사용한다.
- 기록 시각은 `YYYY-MM-DD HH:mm:ss` 형식으로 적는다.
- 작업 요청 요약, 수정 전 상태, 수정한 내용, 수정 후 상태, 테스트 결과, 남은 작업을 반드시 작성한다.
- 실제로 테스트하지 않은 내용은 테스트했다고 쓰지 않는다.
- 실패하거나 미완성인 내용도 숨기지 않고 기록한다.

---

## 6. 코드 작성 기준

### Unity / C# 기준

- MonoBehaviour의 역할을 명확히 분리한다.
- 입력, 이동, 자원, UI, 업그레이드, NPC 로직을 가능한 한 분리한다.
- Update에 모든 로직을 몰아넣지 않는다.
- 반복 탐색이 필요한 경우 캐싱한다.
- `FindObjectOfType`, `GameObject.Find` 남발을 피한다.
- 모바일 성능을 고려한다.
- 자주 생성/삭제되는 오브젝트는 Object Pooling을 우선 고려한다.
- 자원 스택은 물리 기반으로 쌓지 말고, 계산된 슬롯 위치를 사용한다.
- Tween 또는 Coroutine으로 자원 이동 애니메이션을 처리한다.
- 외부 패키지가 없는 경우 Coroutine 기반 구현을 우선한다.

---

## 7. 커뮤니케이션 규칙

- 답변은 간결하되 필요한 정보는 빠뜨리지 않는다.
- 안 되는 것은 안 된다고 말한다.
- 불확실한 내용은 불확실하다고 말한다.
- 사용자가 원하지 않은 장황한 설명은 피한다.
- 코드 전체를 다시 보여주기보다, 필요한 경우 수정된 부분 중심으로 보여준다.
- 단, 새 파일을 생성한 경우에는 전체 내용을 보여줘도 된다.
- 사용자가 특정 형식을 요구하면 그 형식을 우선한다.

---

## 8. 금지 사항

- 사용자 허락 없이 파일 수정 금지
- 사용자 허락 없이 대규모 리팩터링 금지
- 요청과 관계없는 기능 추가 금지
- 임의로 기획 변경 금지
- 하드코딩 후 설명 없이 방치 금지
- 테스트하지 않은 내용을 테스트 완료라고 말하기 금지
- 기존 작업 로그 삭제 금지
- WORKING_CONTEXT.md 갱신 누락 금지
- RULES.md 규칙 무시 금지
