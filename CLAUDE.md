# Pocket Forge Claude Code Guide

작업 전 `RULES.md`, `WORKING_CONTEXT.md`, `TASKS.md`, `PROJECT_PLAN.md`, `AI_USAGE.md`를 읽는다.

루트 문서의 역할과 참조 순서는 `DOCUMENTATION.md`를 따른다.

- 사용자의 명확한 승인 전에는 파일을 수정하지 않는다.
- `TASKS.md`의 현재 작업 범위를 벗어나지 않는다.
- 작업 완료 후 `WORKING_CONTEXT.md`와 `TASKS.md`를 반드시 갱신한다.
- 외부 패키지, SDK, 배포, 계정 및 시스템 설정 변경은 먼저 승인받는다.

세부 규칙은 `RULES.md`, Codex와 공유하는 운영 규칙은 `AGENTS.md`를 따른다.
